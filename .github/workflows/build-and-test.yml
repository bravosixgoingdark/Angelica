
name: Build and test

on:
  pull_request:
    branches: [ master, main, 2.x-celeritas ]
  push:
    branches: [ master, main, 2.x-celeritas ]

jobs:
  build-and-test:
    uses: GTNewHorizons/GTNH-Actions-Workflows/.github/workflows/build-and-test.yml@master
    secrets: inherit
